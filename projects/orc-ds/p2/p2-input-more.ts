import { AfterViewInit, ChangeDetectionStrategy, Component, Directive, ElementRef, HostListener, input, model, output } from '@angular/core';
import { P2_SHARED_STYLES } from './p2-shared';

@Component({ selector: 'orc-input-color', standalone: true, template: `<label class="orc-input-color" [attr.aria-label]="ariaLabel()"><input type="color" [value]="value()" [disabled]="disabled()" (input)="onInput($event)" /><input class="text" type="text" [value]="value()" [disabled]="disabled()" (input)="onTextInput($event)" [attr.aria-label]="ariaLabel()" /></label>`, styles: [P2_SHARED_STYLES + `.orc-input-color{display:inline-flex;align-items:center;gap:.5rem}.orc-input-color input[type=color]{width:2.5rem;height:2.5rem;padding:.15rem;border:1px solid var(--orc-component-border-strong);border-radius:.4rem;background:var(--orc-component-surface)}.orc-input-color .text{width:7rem;padding:.5rem;border:1px solid var(--orc-component-border-strong);border-radius:.4rem}`], changeDetection: ChangeDetectionStrategy.OnPush })
export class InputColorComponent { readonly value = model('#3b82f6'); readonly disabled = input(false); readonly ariaLabel = input('Color'); readonly valueChangeEvent = output<string>(); onInput(event: Event): void { this.update((event.target as HTMLInputElement).value); } onTextInput(event: Event): void { this.update((event.target as HTMLInputElement).value); } private update(value: string): void { this.value.set(value); this.valueChangeEvent.emit(value); } }

@Directive({ selector: '[orcAutoFocus]', standalone: true })
export class AutoFocusDirective implements AfterViewInit { readonly disabled = input(false); constructor(private readonly element: ElementRef<HTMLElement>) {} ngAfterViewInit(): void { if (!this.disabled()) queueMicrotask(() => this.element.nativeElement.focus()); } @HostListener('keydown.escape', ['$event']) onEscape(event: Event): void { if (this.disabled()) event.stopPropagation(); } }
