import { ChangeDetectionStrategy, Component, input, output } from '@angular/core';
export interface ListItem { id:string; label:string; description?:string; disabled?:boolean; selected?:boolean; }
@Component({selector:'orc-list',standalone:true,templateUrl:'./list.component.html',styleUrl:'./list.component.scss',changeDetection:ChangeDetectionStrategy.OnPush,host:{'class':'p-listbox p-component','[attr.id]':'id() || null','[attr.data-pc-name]':"'listbox'"}})
export class ListComponent { readonly items=input<ListItem[]>([]); readonly label=input('Lista'); readonly selection=input<'none'|'single'|'multiple'>('none'); readonly id=input<string | undefined>(undefined); readonly styleClass=input(''); readonly itemSelect=output<ListItem>(); select(item:ListItem):void{if(!item.disabled)this.itemSelect.emit(item);} }
